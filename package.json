{
  "name": "almirah-production-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "login": "clasp login",
    "push": "cd apps-script && clasp push",
    "open": "cd apps-script && clasp open",
    "deploy": "cd apps-script && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
